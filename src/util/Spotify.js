import {credentials} from '../config/credentials';

const accessToken = '';
const tokenType = '';
const expiresIn= 0;
const refreshToken= '';

export const Spotify = {
    search(term) {
        fetch() {

        }
    },

    submit(playlist) {

    }
};
